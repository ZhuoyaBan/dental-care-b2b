import { products } from "@/lib/products"; import ProductCard from "@/components/ProductCard"; export default function Page() { return ( <main className="pt-24 min-h-screen"> <div className="max-w-7xl mx-auto px-4 grid grid-cols-1 sm:grid-cols-4 gap-6"> {products.map(p => <ProductCard key={p.id} product={p} />)} </div> </main> ); }
